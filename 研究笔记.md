# GPT-OSS-2T 多轴缩放研究笔记

> **状态:** 持续更新。当前版本基于 2026-09-01 抓取的官方模型配置、NVIDIA 技术文章与 X 讨论。
> **范围:** 研究硬件代理配置与推理系统，不把代理模型误写成已训练、已发布的 OpenAI 模型。

## 一、背景与线索

[SemiAnalysis 原帖](https://x.com/SemiAnalysis_/status/2094470943619842286)从 NVIDIA 图表中识别出三种 Rubin + LPX 分离式推理路径：LPX 承担完整 decode、LPX 只承担 FFN，以及 LPX 充当外部 drafter。图中第一次出现了 `GPT-OSS-2T`、400K cached input、每轮 4K 新上下文和 400 输出 token 的代理工作负载。

![SemiAnalysis 整理的 Rubin + LPX Pareto 曲线](research/scaling01-2094471522228359604/images/01-semianalysis-lpu.jpg)

[Lisan al Gaib 的提问](https://x.com/scaling01/status/2094471522228359604)追问“什么是 GPT-OSS-2T”，随后贴出 NVIDIA 原文截图并给出技术文章链接。截图中的关键句是：Figure 11 展示的是把 GPT-OSS 放大到 2T 参数后，在 Vera Rubin NVL72 与 Groq 3 LPX 上运行的**投影**。

![scaling01 给出的答案截图](research/scaling01-2094471522228359604/images/02-gpt-oss-2t-answer.png)

NVIDIA 原始 Figure 11 如下。图中的 3x/5x 是 Pareto 方向上的投影增益，不是公开 2T checkpoint 的实测结果。

![NVIDIA Figure 11 原图](research/scaling01-2094471522228359604/images/03-nvidia-figure-11.webp)

[SemiAnalysis 后续澄清](https://x.com/SemiAnalysis_/status/2094506450517123226)：GPTOSS 2T 是把 GPT-OSS-120B 的层数、隐藏维度和专家数等配置放大后得到的硬件代理，用来代表硬件团队无法取得真实权重的前沿闭源模型。这个澄清决定了本文的研究方法——我们应比较多种合理的 2T 代理，而不是假设存在唯一真实配置。

完整贴文链、讨论筛选和原始素材见 [X 线索快照](research/scaling01-2094471522228359604/README.md)。

## 二、当前结论

NVIDIA 图中的 `GPT-OSS-2T` 是从 GPT-OSS-120B 架构放大的代理配置，用于在拿不到 GPT-5、Gemini 等真实闭源权重时做硬件架构和 bring-up 研究。它不是一个公开的 2T checkpoint，图中的曲线也是 projection。

因此，研究目标不应是猜一个唯一的“真实 GPT-OSS-2T”，而应建立多个**等总参数、不同激活计算和不同长上下文机制**的候选族，观察 NVIDIA Rubin + LPX 三种分离式推理方案对哪一类架构最有利。

## 三、工业基线与缩放候选总表

<!-- BEGIN GENERATED MODEL COMPARISON -->

> 公开模型使用上游仓库精确 metadata/模型卡；O1–O6 使用 GPT-OSS 形状参数账本。

| 性质 | ID / 模型 | 总参数 | 激活/token | 层数 | Hidden | 专家 / Top-k | 上下文 | Attention | 主要轴 / 用途 |
|---|---|---:|---:|---:|---:|---:|---:|---|---|
| 公开模型 | O0 / GPT-OSS-120B | 116.83B | 5.10B | 36 | 2,880 | 128 / 4 | 131K | Sliding + Full | 校准基线 |
| 公开模型 | D0 / DeepSeek-V4-Pro | 1.599T | 49.00B | 61 | 7,168 | 384 / 6 | 1.05M | CSA + HCA | 49B 激活工业锚点 |
| 公开模型 | K0 / Kimi-K3 | 2.780T | 104.00B | 93 | 7,168 | 896 / 16 | 1.05M | KDA + Gated MLA | 104B 激活工业锚点 |
| 缩放候选 | O1 / 专家数单轴 | 2.066T | 5.36B | 36 | 2,880 | 2304 / 4 | 400K | Sliding + Full | 专家 128 -> 2304 |
| 缩放候选 | O2 / 深度单轴 | 1.999T | 79.24B | 622 | 2,880 | 128 / 4 | 400K | Sliding + Full | 层数 36 -> 622 |
| 缩放候选 | O3 / 宽度主轴 + 小幅增深 | 2.009T | 81.05B | 39 | 11,520 | 128 / 4 | 400K | Sliding + Full | 宽度 4x，层数 36 -> 39 |
| 缩放候选 | O4 / 平衡工业型 / DeepSeek 激活预算 | 1.981T | 52.08B | 64 | 6,144 | 272 / 6 | 400K | Sliding + Full | 深度 + 宽度 + 专家 + top-k |
| 缩放候选 | O5 / Top-16 双目标拟合 | 2.000T | 104.00B | 54 | 5,632 | 328 / 16 | 400K | Sliding + Full | 固定 top-16，联合求解其余离散轴 |
| 缩放候选 | O6 / 自由 Top-N 双目标拟合 | 2.000T | 103.97B | 61 | 4,096 | 416 / 21 | 400K | Sliding + Full | top-N 与层/宽/专家联合求解 |

<!-- END GENERATED MODEL COMPARISON -->

### 参数计数口径

[逐张量参数账本](research/model-scaling/parameter_accounting.py)不使用“约多少 B”的心算，而是分别列出 embedding、LM head、attention 权重/偏置、norm、router、全部专家和激活专家，然后以整数求和。这里的“精确”指**对声明的 GPT-OSS 形状 tensor schema 精确**。

公开 GPT-OSS-120B checkpoint metadata 为 116,829,156,672 个元素；账本按逻辑架构得到 116,789,341,248，相差 39,815,424（0.034%）。这部分差异不会被偷偷摊回候选，可能来自量化 checkpoint 的辅助 scale 或实现专用张量。候选之间统一使用同一逻辑 schema，因此横向比较不受该残差影响。

当前双目标解的整数结果：

- **O5 / 固定 top-16:** 总参数 1,999,859,537,712；激活参数 104,004,704,048。
- **O6 / 自由 top-N:** top-21；总参数 1,999,704,860,448；激活参数 103,973,300,000。

两者都同时逼近 2,000,000,000,000 总参数与 Kimi-K3 的 104,000,000,000 激活参数，但通过不同的层数、宽度、专家宽度和 top-N 达成。

三个模型说明“2T”至少有六个互相独立的轴：

1. **总容量:** 主要由专家权重决定。
2. **激活容量:** top-k、共享专家和 dense/attention 部分决定每 token 实际计算。
3. **顺序深度:** 层数决定不可并行的关键路径。
4. **矩阵宽度:** hidden/intermediate 决定单层 GEMM 规模和 tensor parallel 效率。
5. **长上下文状态:** full/sliding、MLA/KDA、CSA/HCA 对 KV 和 attention FLOPs 的影响远大于总参数量。
6. **并行拓扑:** 专家数与 EP，宽度与 TP，层数与 PP 的可分解性不同。

## 四、第一批 GPT-OSS 形状候选

可复算结果见 [生成候选表](research/model-scaling/candidates.generated.md)。当前先保留六个候选：

- **O1 专家数单轴:** 128 → 2,304 专家，总量约 2.07T，但仍只激活 4 个。它把 active/total 压到约 0.26%，远低于 GPT-OSS、Kimi 和 DeepSeek；路由、EP 元数据和训练覆盖都很可疑，适合作为负对照。
- **O2 深度单轴:** 36 → 622 层，总量约 2T、激活约 79B。顺序关键路径过长，几乎肯定不适合作为低延迟工业模型，但能分离“深度”与“宽度”的影响。
- **O3 宽度主轴:** hidden/intermediate 放大 4 倍并略增到 39 层，总量约 2.01T、激活约 81B。大矩阵对 TP 和 LPX 编译调度更友好，但每 token 计算很重。
- **O4 平衡型 / DeepSeek 激活预算:** 64 层、hidden 6,144、272 专家、top-6，总量约 1.98T、激活约 52B。它最接近 DeepSeek-V4-Pro 的 active budget，是当前最值得先做成本模型的 GPT-OSS 形状候选。
- **O5 Top-16 双目标拟合:** 固定 top-16，再让脚本联合搜索层数、hidden、专家宽度和专家总数；目标同时逼近 2T 总参数与 Kimi-K3 的 104B 激活参数。
- **O6 自由 Top-N 双目标拟合:** 排除 top-16 后，在 top-8 至 top-24 中联合搜索。当前解为 top-21，用来检验“更多、更窄的激活专家”是否比固定 top-16 更合理。

### 工业架构对照候选

- **D1 DeepSeek-shaped 2T:** 保持 61 层、hidden 7,168、top-6 和 CSA/HCA，专家数从 384 增至约 484。由于 top-k 不变，active budget 仍接近 49B。它不是 GPT-OSS 架构，但可能是更可信的 2T 服务代理。
- **K1 Kimi-shaped 2T:** 保持 93 层、hidden 7,168、top-16 和 KDA/MLA，专家数从 896 降到约 640。active budget 仍接近 104B。Kimi 使用 Stable LatentMoE，不能用普通 SwiGLU 专家公式精确反推，需进一步读取实现和权重索引。

## 五、注意力与 400K 上下文轴

NVIDIA Figure 11 使用 cached/total input length 400K、新增上下文 4K、输出 400。这个工作负载下，仅讨论 2T 权重是不够的。

需要至少比较三种 attention family：

| Family | 代表 | 400K 风险 | 对 Rubin + LPX 分离的意义 |
|---|---|---|---|
| Alternating sliding/full | GPT-OSS-120B | full 层仍有大 KV 与 attention 成本 | 最贴近 NVIDIA 的“scaled GPT-OSS”字面定义 |
| KDA + Gated MLA | Kimi-K3 | 状态和算子复杂，但面向 1M context | 可能改变 Rubin attention / LPX FFN 分离的网络比例 |
| CSA + HCA | DeepSeek-V4-Pro | 依赖索引与压缩状态 | 官方声称 1M 下只需 V3.2 的 27% 单 token FLOPs 和 10% KV，可能显著削弱图中 attention offload 的收益 |

这意味着 Figure 11 的 Pareto 曲线很可能只对某个具体的 GPT-OSS attention proxy 成立，不能直接外推到 Kimi-K3 或 DeepSeek-V4-Pro。

## 六、Rubin + LPX 三种模式如何映射候选

1. **Rubin prefill + LPX decode:** 更偏好 active 参数较低、权重可常驻 SRAM、decode GEMM 规则的候选。O1 看似有利，但 2,304 专家的权重不可能整体常驻单个 128GB LPX，跨 rack 专家放置会成为核心问题。
2. **Rubin attention + LPX FFN:** 更偏好 FFN 占 active FLOPs 高、attention/KV 留在 Rubin 的候选。O4/O5 是最直接的对照；KDA/HCA 会改变比例。
3. **LPX drafter + Rubin verifier:** 性能依赖真实 token 分布与接受率。只有 tensor shape 的 2T 代理无法验证 speculative/MTP 质量，最多估算 verifier 和 drafter 的计算/通信上界。

## 七、验证顺序

### S0：参数账本

- 用公开配置和 safetensors metadata 对齐总参数。
- 把 active 参数的定义固定为：LM head + dense attention + router + top-k experts；embedding lookup 不按整矩阵计入。
- 对自定义架构单独写参数公式，禁止套用普通 SwiGLU MoE 公式。

### S1：静态成本模型

- 权重字节：MXFP4/FP4/FP8/BF16 分项。
- 每 token active FLOPs、router FLOPs、attention FLOPs。
- 400K KV/压缩状态占用。
- TP/EP/PP 的最小可整除拓扑。

### S2：分离式推理通信模型

- prefill/decode：每轮 KV 迁移字节与 4K 新上下文增量。
- attention/FFN：每 full-attention layer 的跨 rack 中间状态。
- drafter/verifier：draft tokens、拒绝位置和双侧 KV 状态。

### S3：可复现 Pareto 曲线

- 横轴明确为 steady-state decode TPS/user，另报 TTFT。
- 纵轴明确功耗边界：单 rack、两类 rack 之和或整套系统。
- 同时报告 aggregate TPS、TPS/user、tokens/joule，避免“interactivity”替代全部 latency 指标。

## 八、目前最关键的未知量

1. NVIDIA 实际使用的 GPT-OSS-2T proxy config。
2. 2T 是总 MoE 参数还是另有 dense/vision 组件。
3. 代理的 top-k、共享专家、attention layer pattern 和 active 参数。
4. Figure 11 中每条曲线的 rack 数、batch/concurrency、精度和功耗边界。
5. LPX 128GB SRAM 是单系统还是图中解码配置的总 SRAM。
6. 400K KV 的格式、缓存命中定义和跨 rack 传输方式。
7. speculative decoding 使用的 drafter 尺寸、lookahead 和真实接受率。

## 九、证据与来源

- [OpenAI gpt-oss-120b](https://huggingface.co/openai/gpt-oss-120b)
- [OpenAI 官方 Transformers 指南](https://developers.openai.com/cookbook/articles/gpt-oss/run-transformers/)
- [Moonshot Kimi-K3](https://huggingface.co/moonshotai/Kimi-K3)
- [DeepSeek-V4-Pro](https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro)
- [NVIDIA Groq 3 LPX / Vera Rubin 技术文章](https://developer.nvidia.com/blog/how-nvidia-groq-3-lpx-unlocks-ultrafast-interactivity-at-long-context-on-nvidia-vera-rubin/)
- [X 线索与讨论快照](research/scaling01-2094471522228359604/README.md)
- [参考写作仓库](https://github.com/Sawyer117/dsv4-dspark-article)
